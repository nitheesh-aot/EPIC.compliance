apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ .Chart.Name }}
  name: {{ .Chart.Name }}
data:
  config.js: |-
    // runtime-config.js vars
      window["_env_"] =  {
        "ENV": "dev",
        "NODE_ENV": "production",
        "VITE_APP_API_URL": "{{ .Values.app.api }}",
        "VITE_APP_KEYCLOAK_URL": "{{ .Values.keycloak.url }}",
        "VITE_APP_KEYCLOAK_CLIENT": "{{ .Values.keycloak.client }}",
        "VITE_APP_KEYCLOAK_REALM": "{{ .Values.keycloak.realm }}",
        "VITE_APP_ENV": "{{ .Values.app.env }}",
      }